<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pravin.lede.gl.mypracticeapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:date=".activities.MainActivity" android:label="@string/calculateActivityTitle">
            <intent-filter>
                <action android:date="android.intent.action.MAIN" />

                <category android:date="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>